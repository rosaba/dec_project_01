# dec_project_01

### Before running the code
- Create conda environment from yml:
```conda env create -f environment.yml```

- Activate environment:
```conda activate proj1env```

- Add .env file to project and set X-APP-TOKEN